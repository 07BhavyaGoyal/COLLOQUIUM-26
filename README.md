# COLLOQUIUM'26 - Debugging Challenge

## 🐛 Bug Summary
1. **Easy:** Missing closing tag in the Hero section paragraph.
2. **Mid:** Typo in the navigation attribute (`herf` instead of `href`).
3. **Mid:** Navigation link pointing to a non-existent external file (`events.html`).
4. **Hard:** An invisible `div` with a high `z-index` acting as a click-shield.
5. **Hardest:** Missing `id="home"` on the main section, preventing anchor navigation.
